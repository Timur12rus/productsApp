class Product {
  final int productId;
  String title;
  String productDescription;
  int price;
  String rating;

  Product(this.productId, this.title, this.productDescription, this.price, this.rating);

//  Product(this.productId, this.title, this.productDescription, this.price,
//      this.rating);
}
